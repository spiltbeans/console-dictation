import dictator from './core/main'

export const {
	config,
	system,
	error,
	misc,
	report,
	getConfig,
	getLogNames,
	getPaths,
} = dictator

export default {
	config,
	system,
	error,
	misc,
	report,
	getConfig,
	getLogNames,
	getPaths,
}